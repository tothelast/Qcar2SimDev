# Simlingo-QCar2 Integration Requirements

# Core dependencies
# NumPy pinned to <1.28.0 for compatibility with SciPy 1.11.4
# SciPy 1.11.4 requires numpy<1.28.0,>=1.21.6
# NumPy 2.x removed 'Inf' constant which breaks SciPy 1.11.4
numpy>=1.21.6,<1.28.0
# OpenCV pinned to <4.10.0 for compatibility with NumPy 1.26.4
opencv-python>=4.5.0,<4.10.0
scipy>=1.7.0

# PyTorch (adjust version based on your CUDA version)
torch>=2.0.0
torchvision>=0.15.0

# Transformers for model loading
transformers>=4.30.0

# Hydra for config management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Note: DeepSpeed is NOT required - we use the pre-converted pytorch_model.pt
# from the checkpoint directory instead of loading from ZeRO checkpoint

# PIL for image processing
Pillow>=9.0.0

# Optional: For visualization
matplotlib>=3.5.0

# Note: The QVL library is included in the python/ directory
# and does not need to be installed separately

