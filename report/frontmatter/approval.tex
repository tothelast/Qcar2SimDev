% Signed committee approval page (PDF inserted as page 2; page number rendered
% via the document's \pagestyle{plain}).
% trim = left bottom right top (in pt). The `top` value crops off the page
% number that is baked into the original signed PDF so only LaTeX's
% bottom-center number remains. Adjust the last value if the title block
% gets clipped or some of the number remains visible.
\includepdf[pages=1,pagecommand={\thispagestyle{plain}},clip,trim=0 0 0 60]{frontmatter/signature.pdf}
